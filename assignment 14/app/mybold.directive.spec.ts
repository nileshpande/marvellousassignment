import { MyboldDirective } from './mybold.directive';

describe('MyboldDirective', () => {
  it('should create an instance', () => {
    const directive = new MyboldDirective();
    expect(directive).toBeTruthy();
  });
});
